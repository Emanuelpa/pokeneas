<!DOCTYPE html>
<html>
<head>
  <title>Pokenea: <%= pokenea.nombre %></title>
  <style>
    body { font-family: sans-serif; text-align: center; margin-top: 50px; }
    img { max-width: 300px; border-radius: 10px; }
    .frase { font-style: italic; margin-top: 20px; }
    .container-id { margin-top: 30px; color: gray; font-size: 0.9em; }
  </style>
</head>
<body>
  <h1><%= pokenea.nombre %></h1>
  <img src="<%= pokenea.imagen %>" alt="<%= pokenea.nombre %>" />
  <div class="frase">"<%= pokenea.frase %>"</div>
  <div class="container-id">ID del contenedor: <%= containerId %></div>
</body>
</html>